# requirements.txt — used ONLY by Streamlit Community Cloud.
# Local dev / Docker / CDK use pyproject.toml + uv.
#
# This file pins the runtime deps for the *embedded* (single-process)
# deploy path. Everything FastAPI-/sidecar-related is omitted because
# Streamlit Cloud doesn't host the API anyway.

# AWS + Bedrock
boto3>=1.35
botocore>=1.35

# Config + logging
pydantic>=2.9
pydantic-settings>=2.6
python-dotenv>=1.0
structlog>=24.4
tenacity>=9.0

# LangChain / LangGraph
langchain>=0.3
langchain-core>=0.3
langchain-aws>=0.2
langgraph>=0.2
langgraph-checkpoint>=2.0
langchain-text-splitters>=0.3

# RAG (FAISS in-memory; no Postgres)
faiss-cpu>=1.8
numpy>=1.26
tiktoken>=0.8

# Streamlit + HTTP (httpx kept for the API-mode toggle)
streamlit>=1.39
httpx>=0.27

# Observability — LangFuse Cloud (optional; works without keys)
langfuse>=2.50

# NOTE: we deliberately do NOT include `-e .` here. Streamlit Cloud's
# build environment is unreliable for editable installs of hatch-built
# packages; instead, the `streamlit_app.py` shim at the repo root adds
# `src/` to sys.path before importing `canaid.*`. This works regardless
# of whether the package itself is installed.
